version: "3.8"

services: 
  
  backend: 
    #    build: ./todo_backend
    image: 119972959035.dkr.ecr.us-east-1.amazonaws.com/todo-backend:1.0 
    container_name: todo-backend
    env_file:
      - ./todo_backend/.env
    networks: 
      - mern-network    

  frontend: 
    #    build:
    #      context: ./todo_frontend
    #      args: 
    #        REACT_APP_API_URL: /api/
    image: 119972959035.dkr.ecr.us-east-1.amazonaws.com/todo-frontend:1.0  
    container_name: todo-frontend
    ports: 
      - "80:80"
    depends_on: 
      - backend
    networks:
      - mern-network    
        

networks:
  mern-network:
    driver: bridge
